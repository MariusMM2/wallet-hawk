import {LabelArrayPipe} from './label-array.pipe';

describe('LabelArrayPipe', () => {
    it('create an instance', () => {
        const pipe = new LabelArrayPipe();
        expect(pipe).toBeTruthy();
    });
});
