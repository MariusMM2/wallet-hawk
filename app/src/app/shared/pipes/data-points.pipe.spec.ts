import {DataPointsPipe} from './data-points.pipe';

describe('DataPointsPipe', () => {
    it('create an instance', () => {
        const pipe = new DataPointsPipe();
        expect(pipe).toBeTruthy();
    });
});
