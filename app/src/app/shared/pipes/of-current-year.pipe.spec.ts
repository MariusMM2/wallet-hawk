import {OfCurrentYearPipe} from './of-current-year.pipe';

describe('OfCurrentYearPipe', () => {
    it('create an instance', () => {
        const pipe = new OfCurrentYearPipe();
        expect(pipe).toBeTruthy();
    });
});
