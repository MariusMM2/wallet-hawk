import {OfCurrentMonthPipe} from './of-current-month.pipe';

describe('OfCurrentMonthPipe', () => {
    it('create an instance', () => {
        const pipe = new OfCurrentMonthPipe();
        expect(pipe).toBeTruthy();
    });
});
