import {DivideByMonthPipe} from './divide-by-month.pipe';

describe('DivideByMonthPipe', () => {
    it('create an instance', () => {
        const pipe = new DivideByMonthPipe();
        expect(pipe).toBeTruthy();
    });
});
