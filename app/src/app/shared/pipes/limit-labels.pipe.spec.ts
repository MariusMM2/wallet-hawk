import {LimitLabelsPipe} from './limit-labels.pipe';

describe('LimitLabelsPipe', () => {
    it('create an instance', () => {
        const pipe = new LimitLabelsPipe();
        expect(pipe).toBeTruthy();
    });
});
