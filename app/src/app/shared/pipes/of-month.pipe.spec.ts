import {OfMonthPipe} from './of-month.pipe';

describe('OfMonthPipe', () => {
    it('create an instance', () => {
        const pipe = new OfMonthPipe();
        expect(pipe).toBeTruthy();
    });
});
