import {CategoryPointsPipe} from './category-points.pipe';

describe('CategoryPointsPipe', () => {
    it('create an instance', () => {
        const pipe = new CategoryPointsPipe();
        expect(pipe).toBeTruthy();
    });
});
